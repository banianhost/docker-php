#!/bin/bash

# Run command if any argument is passed
if [ ! $# -eq 0 ] ; then
    $@ 
    exit $?
fi

# Ensure directories exist
mkdir -p /var/www/src/public
mkdir -p /var/www/logs 

# Fix Permissions
fixperms

# Start supervisord
/usr/bin/supervisord -c /etc/supervisord.conf 

