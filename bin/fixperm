#!/bin/bash

# Fix Permissions
mkdir -p /var/www/logs /var/www/tmp /var/www/src

chown www-data:www-data -R /var/www/logs /var/www/tmp /var/www/.composer

# Working on src may take a long time so do it in background
chown -R www-data:www-data /var/www/src &
