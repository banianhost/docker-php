#!/bin/sh

[ -z "$PASSWORD" ] && echo "Need to set PASSWORD To Use Butterfly" && exit 0;

# Set password
echo "www-data:${PASSWORD}" | chpasswd

sudo -u www-data /usr/bin/butterfly.server.py --unsecure --host=0.0.0.0 --port=57575
