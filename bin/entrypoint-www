#!/bin/bash

[ ! -f /var/www/src ] && mkdir -p /var/www/src/public

cd /var/www

# Ensure public directory exists
mkdir -p src/public

# Remove lock file
if [ -f src/.update.lock ] ; then
	rm src/.update.lock
fi

